upstream backend {
    server backend:5000;
}

upstream frontend {
    server frontend:80;
}

server {
    listen 80;

    location /api {
        proxy_pass http://backend/;
    }

    location / {
        proxy_set_header Host $http_host;
        proxy_pass http://frontend/;
    }
}